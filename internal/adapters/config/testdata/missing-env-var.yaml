schema_version: 1
target:
  base_url: http://localhost:8080
  tier: lab
blast_radius:
  max_concurrency: 50
  max_duration: 60s
auth:
  login_endpoint: /login
  credentials:
    username: admin
    password: ${SAPPER_TEST_DEFINITELY_UNSET_VAR}
  token_path: data.access_token
